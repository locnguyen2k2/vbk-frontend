import { Card } from 'antd';
import { AccountForm } from '../../../Organisms/Forms/Accounts/AccountForm';

export const Profile = (props: any) => {
  return (
    <Card>
      <AccountForm />
    </Card>
  );
};